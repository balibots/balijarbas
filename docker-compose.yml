services:
  balijarbas:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: balijarbas
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - TELEGRAM_MCP_HOST=${TELEGRAM_MCP_HOST}
      - MCP_HTTP_PORT=3001
      - TELEGRAM_MCP_API_KEY=${TELEGRAM_MCP_API_KEY:-}
      - CORS_ORIGIN=${CORS_ORIGIN:-*}
    ports:
      - "3001:3001"
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--no-verbose",
          "--tries=1",
          "--spider",
          "http://localhost:3001/health",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
